# 项目核心指令与规范 (Project Rules)

## 1. 项目结构指南
* 需求与架构文档：固定保存在 `docs/` 目录下。
  * `docs/01_requirements.md`: 需求说明
  
* 源码目录：`main/`，硬件驱动与组件放在 `components/`。

## 2. 工作工作流 (Workflow)
* 在执行任何新功能开发前，**必须先阅读 `docs/` 下的最新文档**。
* 代码修改完成后，如果修改了整体架构，请同步更新 `docs/` 中的相关说明。
* 编写代码时请遵守 ESP-IDF v5.1 及以上规范，保持模块化。

## 3. 编程与代码生成约定
* 使用 C99 标注风格，注释必须清晰完整。
* 遇到报错时，先定位问题根源并说明原因，再进行文件修改。